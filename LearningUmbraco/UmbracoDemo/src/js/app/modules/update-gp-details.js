//import gpDetailsLookup from '../services/gp-details-lookup';

//function updateGpDetails(tagGp) {

//    //model binding values
//    const query = document.getElementById(`${tagGp}-Practice`);
//    const practiceId = document.getElementById(`${tagGp}-PracticeId`);
//    const gpList = document.getElementById(`${tagGp}-GpList`);
//    const gpId = document.getElementById(`${tagGp}-GpId`);

//    //static values
//    const practiceList = document.getElementById(`${tagGp}-practice-list`);

//    if (typeof query !== 'undefined' && query !== null
//        && typeof practiceId !== 'undefined' && practiceId !== null
//        && typeof gpList !== 'undefined' && gpList !== null
//        && typeof gpId !== 'undefined' && gpId !== null
//        && typeof practiceList !== 'undefined' && practiceList !== null
//        ) {       
//        gpDetailsLookup(query, practiceList, practiceId, gpList, gpId);
//    }

//}

//export default updateGpDetails;